const defaultState = {
  tableName: null,
  columns: [
    {name: '', type: ''}],
  primaryKeys: [''],
  ongoingRequest: false,
  lastError: null,
  lastSuccess: null
};

export default defaultState;
